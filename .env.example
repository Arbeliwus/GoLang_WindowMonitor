APP_ENV=development
HTTP_ADDR=:8080

# 方式 A：單一 DSN（url dsn）
# DB_URL=postgres://postgres:REPLACE_ME@127.0.0.1:5432/iot_evergrain?sslmode=disable

# 方式 B：組件式連線（key=value，推薦）
DB_USER=postgres
DB_PASS=REPLACE_ME
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=iot_evergrain
DB_SSLMODE=disable
